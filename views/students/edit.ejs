<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Student - Edit</title>
</head>

<body>

    <h1>Edit a Student's Info</h1>

    <form action="/students/update" method="POST">
        <input type="hidden" name="student_id" value="<%= student.id %>" />

        <div>
            <label for="fname">First Name</label>
            <input type="text" name="first_name" id="fname" value="<%= student.first_name %>" required>
        </div>
        <br>

        <div>
            <label for="lname">Last Name</label>
            <input type="text" name="last_name" id="lname" value="<%= student.last_name %>" required>
        </div>
        <br>

        <div>
            <label for="class">Class</label>
            <input type="text" name="class" id="class" value="<%= student.class %>" required>
        </div>
        <br>

        <button type="submit">Save information</button>
        <a href="/students/list">
            <button type="button">
                Cancel
            </button>
        </a>
    </form>
</body>

</html>